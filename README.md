# unlearning_benchmark

A framework for machine unlearning evaluation and benchmarking.

## Installation

You can set up the environment and install the package using conda:

```bash
conda env create -f environment.yml
conda activate unlearning_benchmark
pip install -e ".[dev]"
```

## Testing

To run the tests:

```bash
pytest tests/
```
